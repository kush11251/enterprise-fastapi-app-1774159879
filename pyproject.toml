[tool.poetry]
name = "enterprise_fastapi"
version = "0.1.0"
description = "Advanced multi-layered FastAPI app"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.110.0"
uvicorn = "^0.24.0
pydantic = "^1.10.0"
pytest = "^7.0.0"
